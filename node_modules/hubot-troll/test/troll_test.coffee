chai = require 'chai'
sinon = require 'sinon'
chai.use require 'sinon-chai'

expect = chai.expect

describe 'troll', ->
  beforeEach ->
    @robot =
      respond: sinon.spy()
      messageRoom: sinon.spy()
      logger:
        info: sinon.spy()

    require('../src/troll')(@robot)

  # it 'registers a respond listener', ->
  #   expect(@robot.respond).to.have.been.calledWith(/hello/)
  #
  # it 'registers a hear listener', ->
  #   expect(@robot.hear).to.have.been.calledWith(/orly/)

  it 'messages the room', ->
    expect(@robot.messageRoom).to.have.been.called()
